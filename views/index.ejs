<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white">
<div class="lg:w-full md:w-1/2 bg-gray-100 rounded-lg p-8 flex flex-col">
    <h2 class="text-gray-900 text-lg font-medium title-font mb-5">Add Note</h2>


    <form action="/create" method="post">
        <div class="relative mb-4">
            <label for="title" class="leading-7 text-sm text-gray-600">Title</label>
            <input type="text" id="title" name="title"
                   class="w-full bg-white rounded border border-gray-300 focus:border-indigo-500 focus:ring-2 focus:ring-indigo-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
        </div>
        <div class="relative mb-4">
            <label for="description" class="leading-7 text-sm text-gray-600">Description</label>
            <textarea id="description" name="description"
                      class="w-full bg-white rounded border border-gray-300 focus:border-indigo-500 focus:ring-2 focus:ring-indigo-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out h-32 resize-none"></textarea>
        </div>
        <button type="submit"
                class="text-white bg-indigo-500 border-0 py-2 px-8 focus:outline-none hover:bg-indigo-600 rounded text-lg">Add
            Note
        </button>
        <p class="text-xs text-gray-500 mt-3">Created by Papa</p>
    </form>
</div>

<!-- tasks -->

<div class="container px-5 py-24 mx-auto">
    <div class="flex flex-wrap -m-4">
        <% if(files.length > 0) { %>
            <% files.forEach(function(val) { %>
                <!-- Card -->
                <div class="p-4 lg:w-1/3">
                    <div class="lg:w-full md:w-full bg-gray-100 rounded-lg p-8 flex flex-col">
                        <div class="h-full bg-gray-100 bg-opacity-75 px-8 pt-16 pb-24 rounded-lg overflow-hidden text-center relative">
                            <h1 class="tracking-widest text-m title-font font-medium text-gray-400 mb-1">Title</h1>
                            <!-- Use file.title and file.description to display the title and description -->
                            <h1 class="text-zinc-600 title-font sm:text-2xl text-xl font-medium text-gray-900 mb-3"><%= val %></h1>
                            <p class="leading-relaxed mb-3">Click to Expand</p>
                            <a href="/file/<%= val %>" class="text-indigo-500 inline-flex items-center">Read More..➡️</a>
                            <a href="/edit/<%=val %>" class="text-zinc-400 mx-2">Edit📝</a>
                        </div>
                    </div>
                </div>
            <% }) %>
        <% } else { %>
            <h1 class="text-white">No Task Here.</h1>`
        <% } %>
    </div>
</div>

</body>
</html>
